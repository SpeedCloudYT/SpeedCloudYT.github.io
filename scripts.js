/* remove the comment to re-enable the rickroll
const titles = ["Never gonna give you up", "Never gonna let you down", "Never gonna roll around", "And hurt you"];
let index = 0;

function changeTitle() {
    document.title = titles[index];
    index = (index + 1) % titles.length;
}

setInterval(changeTitle, 5000);
*/

